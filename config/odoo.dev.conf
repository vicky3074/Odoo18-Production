[options]
; Master password for database operations
admin_passwd = w6kh-tdxq-ar2z

; Database connection
db_host = db
db_port = 5432
db_user = odoo
db_password = myodoo

; Addons path - include custom addons
addons_path = /mnt/extra-addons,/mnt/custom-addons

; Data directory
data_dir = /var/lib/odoo

; Logging configuration for development
logfile = /var/log/odoo/odoo.log
log_level = debug
log_handler = :DEBUG

; Development settings
dev_mode = all
without_demo = False

; Server configuration
xmlrpc_port = 8069
longpolling_port = 8072

; Performance settings for development
workers = 0
limit_time_cpu = 60
limit_time_real = 120

; Auto-reload on file changes
auto_reload = True